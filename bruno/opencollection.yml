opencollection: 1.0.0

info:
  name: Preppy API
bundled: false
extensions:
  bruno:
    ignore:
      - node_modules
      - .git
      - auth
      - auth/**
      - service-account.json 
      - .env