name: core_state
description: Base ViewModel, UI events, phase types, and Flutter StateViewModel bridge.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.11.5
  flutter: ">=3.41.0"

resolution: workspace

dependencies:
  core_models:
    path: ../models
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.3.1
  go_router: ^14.6.1
  riverpod: ^3.2.1

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
