name: core_state
description: Presentation-layer ScreenState and BaseViewModel helpers for Riverpod feature ViewModels.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.11.5

resolution: workspace

dependencies:
  core_models:
    path: ../models
  meta: ^1.17.0
  riverpod: ^3.2.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
