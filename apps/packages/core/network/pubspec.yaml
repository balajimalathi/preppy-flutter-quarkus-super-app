name: core_network
description: Dio-based HTTP client + interceptors (token refresh, retry, logging) for Preppy.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.11.5

dependencies:
  dio: ^5.7.0
  riverpod: ^2.5.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
