name: core_network
description: Dio-based HTTP client + interceptors (token refresh, retry, logging) for Preppy.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.11.5
  flutter: '>=3.41.0'

resolution: workspace

dependencies:
  connectivity_plus: ^6.1.4
  dio: ^5.9.2
  flutter:
    sdk: flutter
  riverpod: ^3.2.1

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  lints: ^6.0.0

flutter:
  uses-material-design: true
