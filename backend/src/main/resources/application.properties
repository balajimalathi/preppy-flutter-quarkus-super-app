quarkus.application.name=preppy-backend

# ---- HTTP --------------------------------------------------------------------
quarkus.http.port=8080
quarkus.http.cors.enabled=true
quarkus.http.cors.origins=https://preppy.skndan.com

# ---- Datasource (Postgres) ---------------------------------------------------
quarkus.datasource.db-kind=postgresql
quarkus.datasource.username=${POSTGRES_USER:postgres}
quarkus.datasource.password=${POSTGRES_PASSWORD:postgres}
quarkus.datasource.jdbc.url=${POSTGRES_URL:jdbc:postgresql://localhost:5432/preppy}

# ---- Hibernate / Flyway ------------------------------------------------------
quarkus.hibernate-orm.database.generation=validate
quarkus.flyway.migrate-at-start=true
quarkus.flyway.baseline-on-migrate=true
quarkus.flyway.locations=classpath:db/migration

# ---- JWT (Firebase) ----------------------------------------------------------
# Firebase issues RS256 tokens; smallrye-jwt only needs the issuer and JWKS URL.
# The Admin SDK (used by AuthFilter) still does the heavy lifting on verification.
mp.jwt.verify.issuer=https://securetoken.google.com/${FIREBASE_PROJECT_ID:preppy-dev}
mp.jwt.verify.publickey.location=https://www.googleapis.com/robot/v1/metadata/x509/[email protected]
firebase.project-id=${FIREBASE_PROJECT_ID:preppy-dev}
firebase.service-account.path=${FIREBASE_SERVICE_ACCOUNT_PATH:/etc/preppy/firebase-service-account.json}

# ---- Redis -------------------------------------------------------------------
quarkus.redis.hosts=${REDIS_URL:redis://localhost:6379}

# ---- Qdrant ------------------------------------------------------------------
qdrant.host=${QDRANT_HOST:localhost}
qdrant.port=${QDRANT_PORT:6334}
qdrant.use-tls=${QDRANT_USE_TLS:false}

# ---- LLM ---------------------------------------------------------------------
llm.provider=${LLM_PROVIDER:none}
llm.api-key=${LLM_API_KEY:}

# ---- OpenAPI / Swagger UI ----------------------------------------------------
quarkus.smallrye-openapi.path=/openapi
quarkus.swagger-ui.always-include=true
quarkus.swagger-ui.path=/swagger

# ---- Container image ---------------------------------------------------------
quarkus.container-image.group=preppy
quarkus.container-image.name=backend
quarkus.container-image.tag=${project.version}
